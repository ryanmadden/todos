{
  "name": "todo-firebase-stripe",
  "version": "1.0.0",
  "description": "Todo SaaS with Stripe subscriptions integration.",
  "main": "/public/index.html",
  "scripts": {
    "dev": "firebase serve",
    "deploy": "firebase deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
